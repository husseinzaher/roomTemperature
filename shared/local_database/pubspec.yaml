name: local_database
description: "Local database: the Drift (SQLite) store for readings, daily averages, and settings"
version: 0.1.0+1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0
  flutter: ^3.44.0

dependencies:
  drift: ^2.34.3
  drift_flutter: ^0.3.1
  flutter:
    sdk: flutter

dev_dependencies:
  build_runner: ^2.16.0
  drift_dev: ^2.34.5
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.3.0
