targets:
  $default:
    builders:
      drift_dev:
        options:
          # ISO-8601 text keeps sub-second precision and the UTC flag, which
          # the default unix-seconds encoding would both throw away.
          store_date_time_values_as_text: true
