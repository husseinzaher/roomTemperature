name: settings_data
description: "Settings data: the Drift-backed settings repository"
version: 0.1.0+1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  local_database:
    path: ../../../shared/local_database
  settings_domain:
    path: ../settings_domain

dev_dependencies:
  drift: ^2.34.3
  mocktail: ^1.0.5
  test: '>=1.24.0 <=1.31.1'
  very_good_analysis: ^10.3.0
