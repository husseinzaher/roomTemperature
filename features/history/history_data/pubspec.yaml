name: history_data
description: "History data: Firestore-backed history repository"
version: 0.1.0+1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  cloud_firestore: ^6.9.0
  history_domain:
    path: ../history_domain

dev_dependencies:
  fake_cloud_firestore: ^4.0.0
  mocktail: ^1.0.5
  test: '>=1.24.0 <=1.31.1'
  very_good_analysis: ^10.3.0
